# VoiceMap — состояние и план работ

> Документ передачи между сессиями. Актуально на 2026-07-23.
> Читать вместе с `CLAUDE.md` (правила проекта) и `server/RAG_SETUP.md` (настройка поиска).

---

## 1. Что это за продукт (зафиксировано с владельцем)

**VoiceMap — голосовой архив разговоров, где ищешь голосом, а не глазами.**

Ядро: записал → расшифровка со спикерами и таймкодами → **спросил голосом → открылось на нужной секунде**.

Аудитория: предприниматели (митинги, собеседования), журналисты и интервьюеры (точные цитаты), сценаристы и режиссёры.

Главный принцип: **меньше кнопок, меньше экранов**. Всё, что не работает на петлю «найти / записать», — кандидат на удаление.

**Рынок:** цель — США, интерфейс будет английским.

⚠️ **На рынок пока НЕ выходим** (решение владельца, 23 июля). Сейчас улучшаем то, что уже
работает. Отсюда приоритеты: биллинг и хвост локализации отложены — это делается перед
запуском, когда есть куда вести платёж. Надёжность, чистота кода и качество — сейчас.

---

## 2. Где мы сейчас

Всё перечисленное **сделано и задеплоено** в `main` → Railway (`https://voicemap1-production.up.railway.app`).

| Сделано | Суть |
|---|---|
| Аудит и починка | 10 критических багов клиента, 3 дыры безопасности, тексты, цвета, доступность, бандл 1.4МБ→102КБ |
| Расчистка | Удалены: mood, Spaces, экран Аналитики с геймификацией, карта-космос, recharts |
| Голосовой поиск (RAG) | Чанкинг + эмбеддинги + Firestore vector search + `POST /api/ai/search` |
| Поиск = главный экран | `SearchHero`: «Спроси свои записи», кнопка «Найти голосом», ответ с источниками |
| Прыжок к моменту | Клик по источнику открывает запись и перематывает на нужную секунду |
| Синхронный плеер | Тап по реплике играет с неё, подсветка активной, автоскролл, подсветка найденного момента |
| Бэкфилл архива | Кнопка в Настройках: индексирует старые записи пачками |
| Импорт аудио | Кнопка «Загрузить аудио» — вход для очков, диктофонов, любых файлов |
| Учёт и лимиты расшифровки | Счётчик минут по пользователю за месяц, отказ по лимиту, карточка расхода в Настройках |
| Экспорт цитаты с таймкодом | Выделил текст в транскрипте → скопировал со спикером, таймкодом и источником |
| Локализация (частично) | Язык вывода AI из настроек + механизм переводов, ключевые экраны на английском |
| Согласие на запись | Предупреждение перед первой записью + постоянное напоминание на экране записи |
| Follow-up по задачам | Карточка «Обещал и не сделал»: протухшие обещания и просроченные сроки |
| Обложки записей | Шесть текстур, выбор по хешу id — запись всегда выглядит одинаково |
| Надёжность загрузки | Ретраи при сбое сети + статус ошибки не затирает готовую расшифровку |

Состояние кода: `tsc`, `eslint`, **288 тестов**, сборка — зелёные.

---

## 3. ✅ Блокеры сняты — поиск работает (проверено 2026-07-23)

Всё, что раньше висело здесь, закрыто и проверено на бою:

1. ✅ **Векторный индекс в Firestore** — существует, статус Enabled. Коллекция `chunks`, поле `embedding`, Index ID `CICAgOjKh4EK`.
2. ✅ **Модель эмбеддингов `gemini-embedding-001`** — работает. Проверка фактом: запрос «про дрова» на проде вернул осмысленный ответ с двумя источниками и таймкодом.
3. ✅ **Бэкфилл архива** — прогнан 23 июля, у записей проставлен `ragIndexedAt`.
4. ✅ **Правила Firestore опубликованы** 23 июля. Важно: до этого дня на бою лежали правила **от 25 апреля** — файл `firestore.rules` из репозитория не деплоился ни разу. Теперь совпадает с репозиторием.

**Как проверять, что поиск жив:** открыть прод → спросить что-нибудь в поле поиска → должен прийти ответ со списком источников. Если пусто или ошибка — смотреть логи Railway.

⚠️ **Правила Firestore по-прежнему не деплоятся вместе с кодом.** `git push` их не трогает. При правке `firestore.rules` — руками: консоль Firebase → Firestore → Rules → вставить → Publish (либо `firebase deploy --only firestore:rules`, если поставить CLI).

---

## 4. Очередь задач (по приоритету)

### 4.1. ✅ Учёт и лимиты расшифровки — СДЕЛАНО
Как устроено (подробности — раздел 8):
- Счётчик секунд аудио: `users/{uid}/usage/{YYYY-MM}`, пишет только сервер.
- Тариф: `users/{uid}.plan` (`free` / `pro` / `team`), по умолчанию `free`. Лимиты в env.
- Лимит применяется во всех трёх точках расшифровки: `/process-recording`, `/ai/transcribe`, `/ai/retranscribe`.
- В Настройках — карточка «Использовано 8 ч из 20 ч» с прогресс-баром.

Осталось по этой теме:
- **Назначать тариф `pro`/`team` пока нечем** — поле `plan` проставляется вручную в консоли Firestore. Биллинг (Stripe) — отдельная задача.
- Экрана апгрейда нет: тост говорит «после апгрейда тарифа», но вести некуда.

### 4.2. ✅ Решение по Deepgram — ЗАКРЫТО: остаёмся на Gemini
Владелец проверил таймкоды на реальных записях (2026-07-23): расхождение приемлемое,
синхронный плеер попадает в нужный момент. **Deepgram не берём.**

Почему это важно зафиксировать: Deepgram ≈ $0.38/час против $0.14/час у Gemini — втрое дороже.
При 1000 активных юзеров это разница ~$4 800/мес. Решение не переоткрывать без новых фактов
(например, жалоб пользователей на промах плеера по длинным записям).

### 4.3. ✅ Экспорт цитаты с таймкодом — СДЕЛАНО
Выделяешь текст в транскрипте (обычным выделением, без отдельного «режима цитирования») →
всплывает панель → «Скопировать цитату» или скачать файлом.

- Одна реплика: `«текст»` + `— Спикер · 12:34 · Название, дата`.
- Несколько: диалог `Спикер [12:34]: текст` + источник в конце.
- Частичное выделение уважается: в цитату попадает ровно выделенный кусок, а не вся реплика.
- Работает и в кратком режиме транскрипта (индексы берутся от отрисованного списка).
- Файлы: `src/lib/quoteExport.ts` (формат), `src/hooks/useTranscriptSelection.ts` (выделение → реплики),
  `src/hooks/useQuoteExport.ts` (действия), `src/components/recording/QuoteToolbar.tsx` (панель).

### 4.4. ✅ Follow-up по задачам — СДЕЛАНО
Карточка «Обещал и не сделал» на дашборде. Показывает **только протухшие** обещания,
в отличие от карточки Фокуса (та про сегодня).

Что считается долгом:
- невыполненная задача из записи старше 3 дней, **или**
- задача с уже прошедшим дедлайном — даже если запись вчерашняя.

Сортировка: сначала просроченные по сроку, дальше самые старые. Чисто — карточка не рендерится вовсе.
Галочка отмечает выполнение прямо оттуда, дедлайн показывается по-человечески («срок был 28 апреля»),
ответственные подтягиваются из `richActionItems`.

Файлы: `src/lib/followUp.ts` (сборка и сортировка), `src/components/dashboard/FollowUpCard.tsx`.

⚠️ **Грабля, найденная тестом:** `new Date('2026-04-27')` разбирается как UTC-полночь, и западнее
Гринвича (Аризона — UTC-7) это уже 26-е число. Дедлайны съезжали на сутки вместе с признаком
«просрочено». ISO-даты теперь разбираются вручную как локальные. Если где-то ещё появится
`new Date(строка)` с голой датой — та же ловушка.

### 4.5. Локализация под США — В РАБОТЕ (сервер готов, интерфейс наполовину)

**✅ Сервер — сделано.** Язык вывода AI берётся из настроек пользователя, а не захардкожен.
Клиент передаёт только код из закрытого списка (`'ru' | 'en'`), промпт целиком собирается
на сервере (`server/lib/lang.ts`). Переведены саммари, идеи, задачи, ключевые моменты,
метки спикеров, совет дня, ответ поиска, недельный дайджест.
**Речь в транскрипте не переводится никогда** — это цитаты, а не пересказ.

**✅ Механизм переводов — сделано.** Свой словарь вместо react-i18next (бандл дороже пользы):
`src/i18n/ru.ts` — источник истины по ключам, `en.ts` типизирован по нему, забытый перевод =
ошибка компиляции. Хук `useT()`, провайдер в `App.tsx`.

**✅ Переведено:** вход, обе навигации, экран записи, согласие на запись (включая названия
штатов), поиск, лимит расшифровки, «Обещал и не сделал», цитаты, часть Настроек.
Локализованы и форматтеры дат/длительностей — иначе внутри английской фразы оставались
русские склонения.

**⬜ Осталось перевести:** дашборд (карточки Фокуса, Идей, дайджеста, недавних записей),
библиотека записей, экран записи целиком (детали, транскрипт), заметки, напоминания,
остаток Настроек, ассистент. Порядка 1200 строк в ~80 файлах.

⚠️ **Дефолт языка — русский.** Меняется в двух местах: `defaultAppSettings.language`
(`src/types/index.ts`) и `DEFAULT_LANG` (`server/lib/lang.ts`). Перед запуском на США — `'en'`.

⚠️ Язык дублируется в localStorage (`voicemap_lang`), потому что экран входа рендерится
до загрузки настроек из Firestore. Выбор до входа переносится в профиль.

### 4.6. ✅ Согласие на запись — СДЕЛАНО (минимум)
- Перед **первой** записью — модалка «Предупреждай о записи»: какие штаты, что сказать вслух, и прямая оговорка «это не юридическая консультация».
- Отметка о прочтении — `AppSettings.consentAcknowledgedAt` (ISO-дата), больше не показывается.
- На экране записи **всегда** висит строка «Скажи вслух, что идёт запись» — в том числе во время записи, потому что объявлять надо сразу после старта.
- В Настройках → Запись есть строка «Согласие на запись», открывает то же предупреждение только на чтение.
- Файлы: `src/lib/consent.ts` (список штатов + логика), `src/components/recording/ConsentNotice.tsx`.

Не сделано и осознанно отложено: **функция запроса согласия** (проговорить/записать согласие отдельным шагом, отметка «согласие получено» на записи). Текущего предупреждения хватает как минимума, но для продажи корпоративным клиентам этого мало.

⚠️ Тексты предупреждения русские — при локализации (4.5) переводить обязательно, это юридически значимый текст.

### 4.7. Технический долг
✅ **Сделано 23 июля:**
- `TagsView` удалён — был недостижим из навигации, а библиотека делает то же и лучше.
- Ретраи загрузки: `/api/process-recording` шёл одной попыткой, моргнувшая сеть теряла запись
  безвозвратно (аудио живёт только в памяти). Теперь 3 попытки, как в `uploadAudioToR2`.
- Ложный `aiStatus: 'error'` больше не затирает готовую расшифровку.

✅ **Ядро покрыто тестами** (23 июля): `r2` (17 тестов — прежде всего защита от удаления чужих
файлов), `embeddings` (11 — нормализация, батчи, отказы), `vectorStore` (11 — батчи по 500,
разбор результатов). 288 тестов вместо 249.

✅ **`server/routes/ai.ts` разделён** (607 строк → шесть файлов по назначению, максимум 154):
`transcribe` / `search` / `backfill` / `assist` / `parse` / `index`. Проверено на бою: все
14 роутов отвечают.

⬜ **Осталось:**
- **~19 файлов больше 200 строк**. Худшие: `App.tsx` 619, `src/lib/api.ts` 496,
  `RecordingDetail.tsx` 426, `SettingsView.tsx` 384.
- `server/lib/auth.ts` без тестов — проверка токена, стоит покрыть.
- Оффлайн-синхронизации Firestore нет (SW есть, persistence не подключён).
- Миграция localStorage использует `batch.set` без merge — узкий edge-case.

---

## 5. Экономика (считано на реальных тарифах, июль 2026)

**Себестоимость расшифровки ≈ $0.14 за час аудио** (Gemini 2.5 Flash: аудио $1.00/1M токенов, 25 токенов/сек = $0.09/час + вывод ≈ $0.05).

**Deepgram Nova-3 ≈ $0.38/час** ($0.0043/мин + диаризация $0.0020/мин) — **втрое дороже**. Замер таймкодов проведён 23 июля: точности Gemini достаточно, Deepgram не берём (см. 4.2).

Маржа при подписке $15/мес с 20 включёнными часами: себестоимость $2.80 → **81%**.

⚠️ Старая оценка в `CLAUDE.md` («~$400/мес на 1000 юзеров») **занижена в 7-10 раз** при активном использовании. Реально: 1000 юзеров × 20 часов = ~$2 800/мес. Но расход зависит от использования, а не от числа регистраций — типично 70% почти не пользуются.

---

## 6. Зафиксированные решения (не переоткрывать без причины)

- **Gemini 2.5 Flash** — основная модель. Deepgram проверен и отклонён 23 июля: точность таймкодов достаточна, а цена втрое выше (см. 4.2).
- **Firestore vector search** — векторная база. Новый сервис не нужен, SDK поддерживает `findNearest` + `FieldValue.vector`.
- **Календарь не делаем** — противоречит идее «ищи голосом, а не листай глазами». Дата = фильтр внутри поиска.
- **Железо (очки, диктофоны) — через универсальный импорт аудио**, а не отдельные интеграции с каждым устройством. Уже сделано.
- **Промпты строятся только на сервере.** Клиент не передаёт промпт/конфиг (иначе это открытый LLM-прокси за чужой счёт).
- **Никакого безлимита, даже в бете.** Добавлять лимит после запуска — больно и вызывает отток. Лимит есть у всех тарифов, включая бесплатный.
- **Расход считает сервер по токенам, а не клиент по длительности.** Клиентскую цифру можно занизить, счётчик токенов Gemini — нет.

---

## 7. Грабли и как здесь всё устроено

**Деплой:** `git push origin main` → Railway пересобирает сам. Правила Firestore **НЕ деплоятся** этим путём — их надо публиковать отдельно (см. раздел 3). Именно поэтому правила три месяца пролежали неактуальными: код ехал, правила нет.

**Firebase CLI на машине владельца не установлен** (на 2026-07-23). Правила публикуются мышкой через консоль. Автоматизировать это ассистентом нельзя: слой безопасности блокирует и скрипты, и нажатия клавиш в боевой админ-консоли — доводить можно только до готовой кнопки.

**Демо-режим:** на экране входа в dev-сборке есть кнопка «Демо-режим (без входа)» — заходит под моковым пользователем с демо-данными, без Firestore. Единственный способ посмотреть интерфейс без реального аккаунта. В проде кнопка не рендерится.

**Урок про ключи (стоил полдня):** симптом «запись висит на Обрабатывается, транскрипции нет» оказался невалидным ключом Gemini, а не багом кода. Проверять ключ фактом, а не на глаз:
```bash
curl -s -o /dev/null -w "%{http_code}" "https://generativelanguage.googleapis.com/v1beta/models?key=КЛЮЧ"
```
200 = живой, 400 = мёртвый. В AI Studio ключ может «выглядеть рабочим», потому что там запросы идут под сессией, а не по ключу.

**Локальный `.env` и переменные Railway — разные места.** Проверять оба. `dotenv` срезает кавычки, Railway — нет. В локальном `.env` нет `FIREBASE_SERVICE_ACCOUNT_JSON`, поэтому локальный сервер работает в урезанном режиме.

**При проблемах на проде первым делом — логи Railway** (Deployments → активный деплой → Logs). Там реальная причина, а не догадки.

**Service worker отдаёт старую версию после деплоя.** Симптом: сервер уже новый (новые роуты отвечают), а интерфейс старый — новых экранов нет. Это не сбой деплоя. Проверять фактом, а не глазами:
```bash
curl -s https://voicemap1-production.up.railway.app/ | grep -o 'assets/index-[^"]*\.js'
curl -s https://voicemap1-production.up.railway.app/assets/index-ХЭШ.js | grep -c "строка из нового кода"
```
Если строка в бандле есть — код задеплоен, виноват кэш. Сброс в консоли браузера:
```js
(await navigator.serviceWorker.getRegistrations()).forEach(r => r.unregister());
(await caches.keys()).forEach(k => caches.delete(k));
```
Потом перезагрузить страницу. На телефоне и других устройствах то же самое — просто закрыть и открыть приложение может не помочь.

**Как выдать себе платный тариф.** Биллинга нет, поле ставится руками: консоль Firebase → Firestore → `users/{uid}` → Add field → `plan` (string) → `pro` (20 часов) или `team` (60 часов). Отсутствие поля = `free`, 2 часа в месяц. Клиенту менять это поле правила запрещают.

---

## 8. Как работает учёт расшифровки

**Файлы:** `server/lib/usage.ts` (арифметика и Firestore), `server/lib/quotaGuard.ts` (обвязка роутов),
`src/lib/usageFormat.ts` (тексты для UI), `src/components/analytics/UsageCard.tsx` (карточка в Настройках).

**Данные в Firestore:**
- `users/{uid}.plan` — `free` (по умолчанию) / `pro` / `team`. Ставится вручную в консоли, клиент менять не может.
- `users/{uid}/usage/{YYYY-MM}` — `{ month, seconds, requests, updatedAt }`. Пишет только сервер.
  Месяц считается по **UTC**, чтобы ключ не поехал при смене региона Railway.

**Лимиты по умолчанию:** free 120 мин, pro 1200 мин (20 ч), team 3600 мин.
Меняются переменными `PLAN_FREE_MINUTES` / `PLAN_PRO_MINUTES` / `PLAN_TEAM_MINUTES` — без пересборки.

**Цикл на каждую расшифровку:**
1. *До* вызова Gemini — `checkQuota`. Отказ, если остаток нулевой или запись заведомо в него не влезает.
2. *После* ответа — `chargeUsage` списывает фактический расход из `usageMetadata` ответа
   (`promptTokensDetails` → модальность AUDIO ÷ 32 токена в секунду; ставка в `GEMINI_AUDIO_TOKENS_PER_SEC`).
   Списывается `max(длительность от клиента, расход по токенам)`.

**Что происходит при отказе:**
- `/ai/transcribe`, `/ai/retranscribe` → **429** с телом `{ error: 'quota_exceeded', usage }`.
  Клиент разбирает его в `QuotaExceededError` (`src/lib/api.ts`) и показывает тост.
- `/process-recording` → **200**, но `queued: false`. Аудио **всё равно грузится в R2**: хранение стоит копейки,
  а терять запись пользователя нельзя — он расшифрует её после апгрейда. Запись получает `aiStatus: 'quota'`.

**Осознанные компромиссы (не баги):**
- *Гонка на предпроверке.* Две одновременные расшифровки могут обе пройти проверку и вместе перебрать лимит.
  Перебор ограничен длиной записи; блокировка ради этого стоила бы транзакции на каждый запрос.
- *Поведение при сбое самой проверки.* В проде — 500 (тихо потерянный лимит = неожиданный счёт, ровно то,
  от чего защищаемся). В деве — пропускаем, чтобы локальный сервер без service account работал. Метка в логах: `[quota]`.
- *Ставка 32 токена/сек* взята с запасом в большую сторону: при `max()` завышенная ставка проигрывает честной
  клиентской длительности, а заниженная позволила бы недоплатить. Экономическая оценка в разделе 5 (25 ток/сек) — про себестоимость, не про списание.
- *Текстовые AI-вызовы (поиск, саммари, идеи) не тарифицируются* — считаем только аудио, это 95%+ расхода.
